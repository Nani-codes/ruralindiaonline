import React from 'react';

const EducationContext = React.createContext({
	padding: '1em',
	titleSize: '40px',
	headerSize: '20px',
	font: '18px',
	joinUs: false,
	navSize: 100,
});

export default EducationContext;
