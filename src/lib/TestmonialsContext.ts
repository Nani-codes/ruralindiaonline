import React from 'react';

const TestMonialsContext = React.createContext({
  padding: "1em",
  titleSize: "40px",
  headerSize: "20px",
  fontSize:'16px',
  joinUs: false,
  navSize: 100,
});

export default TestMonialsContext;